# uber-sample

	npm install
	npm install grunt-cli -g
	npm install grunt -g
	npm install bower -g
	npm install http-server -g
	bower install

#commands
	grunt
	grunt clean
	grunt build
	grunt less
	grunt watch
	
